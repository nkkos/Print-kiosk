# Base URL the kiosk frontend itself uses to reach the QR upload backend
# (server/) — this machine only, so localhost is correct here. The phone
# uses a different, auto-detected LAN URL (see GET /api/config) since it's a
# separate device on the network.
VITE_API_BASE_URL=http://localhost:3001
