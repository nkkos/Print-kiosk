# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# QR upload backend's stored files (dev-only, never committed)
server/uploads/

# Personal Account's permanently-stored files (dev-only, never committed)
server/account-uploads/

# Phone-Camera Scan's raw/processed files (dev-only, never committed — 24h
# retention sweep besides)
server/scans/

# Environment variables (real secrets — .env.example is tracked separately)
.env
.env.*
!.env.example

# Local Claude Code permissions (machine-specific, not project config)
.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
