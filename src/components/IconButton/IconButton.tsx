import styles from './IconButton.module.css';

// Placeholder only — see docs/design/component-library.md, Section 7.
// TODO: icon-only vs icon-with-label, states, accessible naming.
export function IconButton() {
  return <div className={styles.root}>IconButton</div>;
}
